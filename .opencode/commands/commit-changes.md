---
description: Create small, logical git commits from staged and unstaged changes
---

Use the `commit-changes` skill.

Run the workflow from the repository root. If the user supplied arguments, treat them as extra context for the commit packaging request:

```text
$ARGUMENTS
```
