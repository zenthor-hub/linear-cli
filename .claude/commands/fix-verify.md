---
description: Fix failures from bun run verify and related checks
---

Use the `fix-verify` skill.

Run the workflow from the repository root. If arguments were supplied, treat them as extra context for the verification failure:

```text
$ARGUMENTS
```
