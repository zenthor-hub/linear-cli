---
description: Fix failures from bun run verify and related checks
---

# Fix Verification

Use the repository-local `fix-verify` skill.

Follow the skill workflow from the repository root. If the user supplied arguments, treat them as extra context:

```text
$ARGUMENTS
```
