---
description: Create small, logical git commits from staged and unstaged changes
---

# Commit Changes

Use the repository-local `commit-changes` skill.

Follow the skill workflow from the repository root. If the user supplied arguments, treat them as extra context:

```text
$ARGUMENTS
```
